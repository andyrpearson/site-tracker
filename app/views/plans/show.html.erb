<div class="jumbotron">
<p>
  <strong>Name:</strong>
  <%= @plan.name %>
</p>

<p>
  <strong>Price:</strong>
  <%= @plan.price %>
</p>

<table class="table">
  <tr>
    <th>Name</th>
    <th>URL</th>
    <th>Plan</th>
  </tr>

  <% monthly_revenue = 0 %>
  <% @plan.sites.each do |site| %>
  <tr>
    <td><%= site.name %></td>
    <td><%= site.url %></td>
    <td><%= link_to 'Show', site_path(site) %></td>
    <td><%= link_to 'Edit', edit_site_path(site) %></td>
  </tr>
  <% monthly_revenue += site.plan.price %>
  <% end %>
</table>
Monthly recurring revenue:<strong> £<%= monthly_revenue %></strong>
</div>
<p><%= link_to 'Edit', edit_plan_path %></p>
<p><%= link_to 'Show all plans', plans_path %></p>
